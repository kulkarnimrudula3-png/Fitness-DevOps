apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-bluegreen-service
spec:
  type: NodePort
  selector:
    app: aceest-fitness
    version: blue   # Change to green after validation
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30081
